import React from 'react';
import Ladowanie from '../components/ladowanie';

export default {
  title: 'Components/Ladowanie',
  component: Ladowanie,
};

const Template = (args) => <Ladowanie {...args} />;

export const Default = Template.bind({});
Default.args = {};
